# 📚 RAG API -- FastAPI + Qdrant + Gemini AI

This project is a **Retrieval Augmented Generation (RAG) API** that: -
Stores text into a Qdrant vector database - Retrieves relevant context -
Generates answers using Google Gemini AI

## Setup

Create .env: GEMINI_API_KEY=your_key

Run: docker compose up --build
